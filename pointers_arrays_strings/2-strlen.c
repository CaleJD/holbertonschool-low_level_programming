#include "main.h"
#include <string.h>

/**
* _strlen - Gets the length of a variable and
* returns it
* @length: The amount of characters in a variable
* Return: the length
*/
int _strlen(char *s)
{
	int length;
	
	length = strlen(s);
	return (length);
}
