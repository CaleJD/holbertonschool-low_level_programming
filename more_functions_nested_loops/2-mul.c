#include "main.h"

/**
 * mul - multiplies 2 integers together to give a sum
 * @a: digit 1
 * @b: digit 2
 * Return: returns the sum of the process
 */
int mul(int a, int b)
{
	int sum;

	sum = (a * b);
	return (sum);
}
