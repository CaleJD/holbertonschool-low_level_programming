#include "main.h"

/**
 * _abs - Gets the absolute value
 * of an integer
 * @n: The integer to make absolute
 * Return: The absolute value of n
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	return (n);
}
